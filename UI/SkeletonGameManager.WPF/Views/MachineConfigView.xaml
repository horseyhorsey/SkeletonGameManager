<UserControl x:Class="SkeletonGameManager.WPF.Views.MachineConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SkeletonGameManager.WPF.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:ui="clr-namespace:SkeletonGameManager.WPF.Controls"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.Resources>
            <Style x:Key="PinballMatrix" TargetType="{x:Type ItemsControl}">

                <Setter Property="ItemsPanel">
                    <Setter.Value>
                        <ItemsPanelTemplate>
                            <ui:UniformGridWithOrientation Columns="9" Rows="8" IsItemsHost="True"/>
                        </ItemsPanelTemplate>
                    </Setter.Value>
                </Setter>

                <Setter Property="ItemTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <ToggleButton Background="Transparent" BorderThickness="2"
                                    BorderBrush="Black"
                                    >
                                <ToggleButton.Content>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>

                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBox Background="Transparent" Text="{Binding Name}"
                                                 TextWrapping="Wrap"
                                                   HorizontalAlignment="Right"
                                                   />

                                        <TextBox Background="Transparent" Text="{Binding Tags}" Grid.Row="1"
                                                 TextWrapping="Wrap"
                                                   HorizontalAlignment="Right"
                                                   />

                                        <TextBlock Text="{Binding Number}" Grid.Row="2"
                                                   HorizontalAlignment="Right"
                                                   />

                                        <StackPanel Orientation="Horizontal" Grid.Row="3">
                                            <CheckBox Content="Stop" IsChecked="{Binding Stop}"/>
                                            <CheckBox Content="Reset" IsChecked="{Binding Reset}"/>
                                        </StackPanel>
                                    </Grid>
                                </ToggleButton.Content>
                            </ToggleButton>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>

            </Style>
            
        </Grid.Resources>

        <TabControl Background="{StaticResource Brush_Paper}">
            <TabItem Header="Switches" >
                <ItemsControl ItemsSource="{Binding Switches}" Style="{StaticResource PinballMatrix}"/>
            </TabItem>

            <TabItem Header="Lamps" Background="{StaticResource Brush_Paper}">
                <ItemsControl ItemsSource="{Binding Lamps}" Style="{StaticResource PinballMatrix}"/>
            </TabItem>

            <TabItem Header="Coils"/>
            
        </TabControl>
    </Grid>
</UserControl>
